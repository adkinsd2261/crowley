# V4.3.3 Historical Ticket Supersession Notice

**Recorded:** 2026-07-28
**Repository baseline:** `ab04924189cde80cb80b341fcbf3256b236497c3`
**Replacement:** V4.3.3R Recovery Kernel + Spark-First Reset

Historical V4.3.3 tickets `#491`–`#495` described a legacy-memory corpus
finalization and database-drain sequence. Do not claim or execute that sequence.
Its assumptions depended on a former live database and historical row counts
that are not authoritative for the current system.

Those ticket rows are absent from the current reset `crowley.db`. Therefore no
live cancellation or status-change events may be fabricated for them. Their
supersession is preserved by this repository notice, the approved
V4.3.3R planning packet, and a current Crowley receipt.

The approved replacement ladder is:

1. R1 — runtime preflight and current-state preservation
2. R2 — schema inventory and migration contract lock
3. R3 — ordered migration runner and baseline adoption
4. R4 — strict startup and SQLite integrity enforcement
5. R5 — versioned recovery bundle and key contract
6. R6 — application-aware restore and recovery failure injection
7. R7 — recovery lock and V4 ladder reconciliation

Only R1 should be activated initially. Later tickets remain dependency-blocked
until their predecessor acceptance and review gates pass.
