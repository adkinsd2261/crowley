# V3.9.7 — Workspace Experience & Reliability

**Status:** Shipped  
**Theme:** The product should feel as considered as the architecture.

---

## Track A — Experience

| Ticket | Deliverable |
|--------|-------------|
| #40 | Intelligence drawer polish — shared workspace tokens, taller panels, glass Project tab |
| #41 | Chat stream readability — user/Crowley distinction, collapse affordance, diagnostics report blocks |
| #44 | Cross-surface cohesion — shared `--workspace-*` tokens across header, compose, drawer |
| #45 | Work surface simplification — Tasks tab demoted after Loops, three-surface docs |

## Track B — Reliability

| Ticket | Deliverable |
|--------|-------------|
| #42 | `CROWLEY_EMBED_PROVIDER` env, lazy embed backfill, health embed/sqlite_vec flags |
| #43 | `requirements-core.txt` / `requirements-ml.txt`, CI embed off, test DB forces embed off |

## Track C — Maintainability & observability

| Ticket | Deliverable |
|--------|-------------|
| #46 | `diagnostics.py` extracted from monolith |
| #47 | `tickets.py` extracted from monolith |
| #48 | `system_metrics` table, 24h summary API, Project tab rollups |
| #49 | `scripts/preflight.py`, doc version lock |

---

## Visual pass checklist

- [ ] Desktop: chat + Intelligence feel like one workspace
- [ ] 390px: drawer tabs scroll, no page overflow
- [ ] User vs Crowley messages distinguishable without bubble UI
- [ ] Diagnostics blocks visually separate from chat
- [ ] Project tab shows 24h operator summary when metrics exist

---

## Operator notes

- Fresh install: `pip install -r requirements-core.txt` + `CROWLEY_EMBED_PROVIDER=off`
- Optional ML stack: `pip install -r requirements-ml.txt`
- Manual embed backfill: `./venv/bin/python3 scripts/backfill_embeddings.py`
- Preflight: `./venv/bin/python3 scripts/preflight.py --quick`
