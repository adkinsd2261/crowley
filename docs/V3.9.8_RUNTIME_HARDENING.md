# V3.9.8 — Runtime Hardening

**Status:** Shipped  
**Theme:** No fragile startup — boot, test, and serve read-only APIs cleanly without Ollama, OpenAI, sqlite-vec, or Torch.

---

## Track — Runtime Hardening

| Ticket | Deliverable |
|--------|-------------|
| #50 | `CROWLEY_TEST_MODE=1` — unified test profile: embed off, model stub, `is_test_mode()` |
| #51 | `probe_model_availability()` — separate reachability truth from routing; graceful chat SSE error |
| #52 | `/api/health` `runtime` block — embeddings, vector_store, retrieval, model, test_mode |
| #53 | sqlite-vec safe fallback — one-time stderr log, `get_sqlite_vec_failure_reason()` |
| #54 | `tests/test_runtime_hardening.py` — fragile startup regression suite |
| #55 | Version bump, ADR-037, doc lock, preflight runtime validation |

---

## Operator notes

Fresh clone with no ML stack:

```bash
pip install -r requirements-core.txt
export CROWLEY_TEST_MODE=1
export CROWLEY_EMBED_PROVIDER=off
python -m unittest discover -s tests -q
```

Live bus health includes a `runtime` block:

```json
{
  "runtime": {
    "embeddings": "off",
    "vector_store": "unavailable",
    "retrieval": "keyword",
    "model": "unavailable",
    "test_mode": false
  }
}
```

Preflight: `./venv/bin/python3 scripts/preflight.py --quick`

---

## Success criteria (met)

1. Core-deps-only install + test mode → tests pass, health shows honest degraded runtime
2. Chat without model → SSE error, user message saved, no stack trace
3. sqlite-vec missing → keyword-only retrieval, operator-visible log once
