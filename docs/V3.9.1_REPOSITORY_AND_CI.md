# Crowley V3.9.1 — Repository & CI

**Status:** Shipped (2026-07-01) · **Last verified:** 2026-07-02
**Release:** `CROWLEY_VERSION = "3.9.1"`, `CROWLEY_RELEASE_LABEL = "Crowley V3.9.1 Repository & CI"`  
**Baseline:** V3.9 Concurrent Ticketing  
**Repository:** [github.com/adkinsd2261/crowley](https://github.com/adkinsd2261/crowley)

---

## 1. Purpose

V3.9 shipped concurrent ticketing but the workspace had no version-control baseline and no automated regression gate. V3.9.1 **grounds the project in git** and **runs the existing unit suite on every push**.

Problems addressed:

1. Handoff `Files Changed` sections were empty without a git baseline.
2. Eighty-eight unit tests exist locally but nothing blocked regressions on `main` before CI.
3. Onboarding docs still listed git init and CI as open gaps.

---

## 2. Shipped surface

### Git repository

| Item | Detail |
|------|--------|
| Remote | `https://github.com/adkinsd2261/crowley` on branch `main` |
| `.gitignore` | `.env`, `crowley.db`, `venv/`, `.crowley/processed/*`, bus logs |
| Handoffs | `crowley_handoff.py --from-git` and `cursor_sync --after` populate file lists |
| Directories | `.crowley/inbox/` and `.crowley/processed/` tracked via `.gitkeep` |

### GitHub Actions CI

| Item | Detail |
|------|--------|
| Workflow | `.github/workflows/tests.yml` |
| Trigger | `push` and `pull_request` to `main` |
| Runner | `ubuntu-latest`, Python 3.12 |
| Command | `python -m unittest discover -s tests -q` |
| Timeout | 20 minutes (embedding model download on cold cache) |

### Documentation

- Full doc sweep: `WHERE_WE_ARE.md`, `PROJECT_STATE.md`, `ROADMAP.md`, `TICKETS.md`, `VERSIONS.md`, `ARCHITECTURE.md`, `ENGINEERING_PRINCIPLES.md`
- ADR-031 in `DECISION_LOG.md`

---

## 3. Explicitly out of scope (V3.9.1)

- First automated canon synthesis (`synthesize_canon.py --write` remains manual — workflow shipped in V3.9.2)
- Remote git collector writing to `memory_items` (V4 theme)

**Later shipped on `main` (Pre-V4 ladder, still under version `3.9.1`):**

- Test DB isolation (`tests/db_helpers.py`, ticket `#13`)
- Agent Feed UI tab (ticket `#19`)
- Ticket detail view (ticket `#20`)

---

## 4. QA checklist

- [ ] `git remote -v` shows `origin` → GitHub
- [ ] `curl -s http://127.0.0.1:8765/api/health` → version `3.9.1`
- [ ] `./venv/bin/python3 -m unittest discover -s tests -q` → **88** tests OK locally
- [ ] GitHub Actions **Tests** workflow green on `main`
- [ ] Dirty-tree handoff: edit a tracked file → `crowley_handoff.py --from-git` lists it

---

## 5. Run commands

```bash
# Local regression
./venv/bin/python3 -m unittest discover -s tests -q

# Handoff with git file list
./venv/bin/python3 scripts/crowley_handoff.py --source cursor --type builder_handoff --from-git

# CI mirrors the workflow file
cat .github/workflows/tests.yml
```

---

## 6. Related documents

- [V3.9_CONCURRENT_TICKETING.md](./V3.9_CONCURRENT_TICKETING.md)
- [WHERE_WE_ARE.md](./WHERE_WE_ARE.md)
- [PROJECT_STATE.md](./PROJECT_STATE.md)
- [VERSIONS.md](../VERSIONS.md)
