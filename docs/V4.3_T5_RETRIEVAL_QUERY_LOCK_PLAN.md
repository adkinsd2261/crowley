# V4.3 T5 — Retrieval + Query Doc Lock (Implementation Plan)

**Status:** Implemented — hand off for QA (#362)  
**Date:** 2026-07-09  
**Ticket:** #362  
**Builder:** Cursor  
**Reviewer:** Codex  
**Depends on:** V4.3 T1–T4 (#358–#361) — **APPROVED** (T4 Codex QA #2465)  
**Codex plan:** APPROVED WITH AMENDMENTS (#2473 / #456)

### Amendments honored

1. Acceptance test 2 primary path = **auto lane inference** (no explicit `lane=`).
2. Matrix setup wording updated for auto-lane; pass criteria add `lane_source` /
   `lanes_used` and keep health exclusion, cap ≤15, `truncated_count`.
3. Lock doc records `#361` **`expand_seed_limit`** probe guard.

---

## Goal

Lock V4.3 Retrieval + Query: ship `docs/V4.3_RETRIEVAL_QUERY_LOCK.md`, unskip and implement acceptance **test 2** (clean finance retrieval), update `docs/TICKETS.md` + matrix overview status, run full unittest discovery.

**Non-goals:** V4.4 chat/`build_prompt`, token budget, truth/security, changing T1–T4 scoring/lane/cap semantics, tools-registry rewrites, version bump.

---

## Deliverables

### 1. Lock doc — `docs/V4.3_RETRIEVAL_QUERY_LOCK.md`

Mirror V4.2 lock structure:

| Section | Content |
|---------|---------|
| Status / tickets | Shipped T1–T5 · #358–#362; `CROWLEY_VERSION` stays `4.1.0` |
| Architecture | `interpret_query` → lane resolve → `retrieve_sparks(query_mode=…)` → depth caps → cognitive context trace |
| Modules table | T1 `cognitive_query.py`; T2 lane inference + secondary boost; T3 scoring profiles; T4 depth caps + `truncated_count` + `expand_seed_limit`; T5 this lock + acceptance |
| Caps | light 8+0; medium **8+7=15**; deep 12+20 explicit; `truncated_count` always present; probe does not seed expansion |
| Profiles | recall ≡ `W_SPARK_*`; planning/decision/reflection weights + soft boosts; numeric-only `score_breakdown`; `score_profile` on result/trace |
| Safeguards | No chat wire; `SPARK_DEFAULT_LIMIT=12` for direct retrieve; `crowley_tools.ToolDefinition` shared contract; facade `crowley.py` unchanged |
| Acceptance | Test 2 Pass |
| QA commands | Focused + full discovery |
| Out of scope | V4.4–V4.6 pointers |

### 2. Acceptance test 2 — `tests/test_v4_acceptance_retrieval.py`

Unskip; implement against fixture `retrieval_finance_query.json`:

1. `IsolatedDbTestCase` — seed fixture `seed_sparks` (money + health) via `sparks.insert_spark` / validated payloads.
2. Call `build_cognitive_context(finance_query, …)` **without** explicit `lanes=` so T2 auto-infers `money` from insurance/subscriptions keywords (also assert with explicit `lanes="money"` as optional second path if useful — **primary path = auto lane** per matrix “auto lane in V4.3”).
3. Assert:
   - all `core_sparks` + `supporting_sparks` have `lane=money`
   - no `health` in results
   - `trace.retrieved_count` ≤ 15
   - `truncated_count` present (int ≥ 0)
4. Do **not** weaken matrix pass-criteria text (Codex #416 / matrix guard).

### 3. Matrix + tickets docs

- `docs/V4_ACCEPTANCE_TEST_MATRIX.md` — overview row test 2 → **Pass** (V4.3 lock); keep full pass-criteria body intact.
- `docs/TICKETS.md` — V4.3 → **Locked**; Current/Next → V4.4; link lock doc; V4.4 unblocked for claiming when directed.

### 4. Hygiene

- Commit #361 working tree (caps + plan + tests + registry limit-omit) if not already on `main` before or as part of the lock commit.
- Do not push unless asked.

---

## Acceptance mapping (#362)

| Criterion | Plan |
|-----------|------|
| Lock doc records modules, caps, profiles | `V4.3_RETRIEVAL_QUERY_LOCK.md` |
| Finance query excludes health | Unskipped acceptance test 2 |
| Full discovery passes | Run `unittest discover -s tests` |
| `docs/TICKETS.md` updated | V4.3 Locked / V4.4 next |

---

## QA command

```bash
CROWLEY_TEST_MODE=1 CROWLEY_EMBED_PROVIDER=off ./venv/bin/python3 -m unittest \
  tests.test_v4_acceptance_retrieval \
  tests.test_v4_acceptance_matrix \
  tests.test_cognitive_query \
  tests.test_cognitive_context \
  tests.test_spark_retrieval \
  tests.test_crowley_tools_contract -q

CROWLEY_TEST_MODE=1 CROWLEY_EMBED_PROVIDER=off ./venv/bin/python3 -m unittest discover -s tests -q
```

---

## Out of scope

| Area | Why |
|------|-----|
| V4.4 #363+ | Next ladder after this lock |
| Chat / `build_prompt` | V4.4 |
| Changing caps/profiles/lanes | Locked in T1–T4 |
| Version bump | Cognitive ladder only |

---

## Codex approval requested

Approve or amend:

1. Lock doc scope/table above.
2. Acceptance test 2 uses **auto lane inference** (no required `lanes=`), with fixture seed money+health.
3. Matrix overview status flip only; full pass-criteria text preserved.
4. #361 hygiene commit may land with the lock commit.
5. No runtime behavior changes beyond enabling the acceptance test.
